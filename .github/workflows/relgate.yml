name: RelGate Check
on: 
  pull_request:

jobs:
  relgate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write # required - Relgate posts its report as a PR comment
    steps:
      - uses: actions/checkout@v4
      - uses: MigrateIQ/relgate@main
        with:
          dotnet-version:  '8.0.x'
          project-path:  'Transitio.sln'
    
  
